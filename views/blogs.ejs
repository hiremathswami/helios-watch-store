<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Helios Watch Guide | Blogs</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/css/header.css">
</head>

<body class="bg-gray-50 font-sans text-gray-800">

  <!-- Header -->
  <%- include('partials/header') %>

  <!-- Page Header -->
  <section class="pt-32 pb-10 text-center">
    <h1 class="text-4xl md:text-5xl font-bold text-blue-900 mb-3">The Watch Guide</h1>
    <p class="text-gray-500 text-lg">Latest stories, reviews & features from Helios experts</p>
  </section>

  <!-- Blog Grid -->
  <section class="max-w-7xl mx-auto px-6 pb-20">
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-10">
      <% blogs.forEach(blog => { %>
        <article class="bg-white shadow-md hover:shadow-2xl transition-all duration-300 rounded-xl overflow-hidden">
          <!-- Blog Image -->
          <div class="overflow-hidden">
            <img src="<%= blog.image %>" alt="<%= blog.title %>"
              class="w-full h-56 object-cover transform hover:scale-105 transition-transform duration-500">
          </div>

          <!-- Blog Details -->
          <div class="p-6">
            <!-- Tag -->
            <p class="uppercase text-sm font-semibold text-red-600 mb-2 tracking-wide"><%= blog.category || 'Feature' %></p>

            <!-- Title -->
            <h3 class="text-2xl font-semibold text-gray-900 mb-3 hover:text-blue-700 transition-colors">
              <a href="<%= blog.link || '#' %>"><%= blog.title %></a>
            </h3>

            <!-- Author & Date -->
            <p class="text-sm text-gray-500 mb-3">
              <span class="font-medium text-gray-700"><%= blog.author || 'Helios Editorial' %></span> • 
              <%= blog.date %>
            </p>

            <!-- Excerpt -->
            <p class="text-gray-600 leading-relaxed mb-4">
              <%= blog.excerpt.substring(0, 130) %>...
            </p>

            <a href="<%= blog.link || '#' %>"
              class="inline-block mt-2 text-blue-700 font-medium hover:underline">
              Read More →
            </a>
          </div>
        </article>
      <% }) %>
    </div>
  </section>

  <!-- Footer -->
  <%- include('partials/footer') %>

</body>
</html>
