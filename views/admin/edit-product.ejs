<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Product | Helios Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 flex">

  <!-- Sidebar (same layout as add product) -->
  <aside class="w-64 bg-[#0f172a] text-white min-h-screen flex flex-col justify-between">
    <div>
      <div class="text-center py-6 text-2xl font-bold tracking-wide border-b border-gray-700">HELIOS Admin</div>
      <nav class="mt-6">
        <a href="/admin/dashboard" class="block py-3 px-6 text-gray-300 hover:bg-gray-800 hover:text-white">Dashboard</a>
        <a href="/admin/manage-products" class="block py-3 px-6 text-gray-300 hover:bg-gray-800 hover:text-white">Products</a>
        <a href="/admin/add-product" class="block py-3 px-6 text-gray-300 hover:bg-gray-800 hover:text-white">Add Product</a>
        <a href="/admin/users" class="block py-3 px-6 text-gray-300 hover:bg-gray-800 hover:text-white">Users</a>
      </nav>
    </div>
    <div class="p-4 text-sm text-center border-t border-gray-700">
      <a href="/" class="text-blue-400 hover:underline">← Back to Store</a>
    </div>
  </aside>

  <!-- Main Content -->
  <main class="flex-1 flex justify-center items-center px-6 py-12">
    <div class="bg-white shadow-lg rounded-2xl p-8 w-full max-w-lg">
      <h1 class="text-2xl font-semibold text-center text-indigo-700 mb-6">Edit Product</h1>

      <form action="/admin/edit-product/<%= product._id %>" method="POST" enctype="multipart/form-data" class="space-y-5">

        <!-- Product Name -->
        <div>
          <label class="block text-sm font-medium text-gray-600 mb-1">Product Name</label>
          <input type="text" name="name" value="<%= product.name %>" required
                 class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500">
        </div>

        <!-- Brand -->
        <div>
          <label class="block text-sm font-medium text-gray-600 mb-1">Brand</label>
          <input type="text" name="brand" value="<%= product.brand %>" required
                 class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500">
        </div>

        <!-- Gender -->
        <div>
          <label class="block text-sm font-medium text-gray-600 mb-1">Gender</label>
          <select name="gender" required
                  class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500">
            <option value="Men" <%= product.gender === 'Men' ? 'selected' : '' %>>Men</option>
            <option value="Women" <%= product.gender === 'Women' ? 'selected' : '' %>>Women</option>
            <option value="Unisex" <%= product.gender === 'Unisex' ? 'selected' : '' %>>Unisex</option>
          </select>
        </div>

        <!-- Movement -->
        <div>
          <label class="block text-sm font-medium text-gray-600 mb-1">Movement</label>
          <input type="text" name="movement" value="<%= product.movement %>" required
                 placeholder="e.g. Quartz, Automatic"
                 class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500">
        </div>

        <!-- Description -->
        <div>
          <label class="block text-sm font-medium text-gray-600 mb-1">Description</label>
          <textarea name="description" rows="3" required
                    class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500"><%= product.description %></textarea>
        </div>

        <!-- Price -->
        <div>
          <label class="block text-sm font-medium text-gray-600 mb-1">Price (₹)</label>
          <input type="number" name="price" value="<%= product.price %>" required
                 class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500">
        </div>

        <!-- Image Upload -->
        <div>
          <label class="block text-sm font-medium text-gray-600 mb-1">Update Image (optional)</label>
          <input type="file" name="image" multiple
                 class="w-full text-gray-600 file:mr-3 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-indigo-600 file:text-white hover:file:bg-indigo-700 cursor-pointer">
          <% if (product.image) { %>
            <img src="<%= product.image %>" alt="Current Image" class="w-24 h-24 object-cover rounded-lg mt-3 border">
          <% } %>
        </div>

        <!-- Buttons -->
        <div class="flex justify-between items-center mt-6">
          <a href="/admin/manage-products"
             class="w-1/2 mr-2 py-3 bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium rounded-lg transition text-center">
            Cancel
          </a>
          <button type="submit"
                  class="w-1/2 ml-2 py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-medium rounded-lg transition">
            Save Changes
          </button>
        </div>

      </form>
    </div>
  </main>

</body>
</html>
